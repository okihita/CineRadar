rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read access to theatres and scraper_runs
    match /theatres/{theatreId} {
      allow read: if true;
      allow write: if false;
    }
    match /scraper_runs/{runId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
